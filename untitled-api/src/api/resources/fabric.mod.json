{
  "schemaVersion": 1,
  "id": "untitled-api",
  "version": "${version}",
  "name": "Untitled Mana API",
  "environment": "*",
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}